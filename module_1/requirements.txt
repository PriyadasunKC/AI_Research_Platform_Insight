flask>=2.3.0
flask-cors>=4.0.0
requests>=2.31.0
pymongo>=4.6.0
python-dotenv>=1.0.0

# ── Optional: SinBERT scorer ────────────────────────────────────────────────
# Only needed if you actually have a trained models/saved/sinbert_scorer.pt
# file and want to use it instead of the rule-based D2-D4 scorer (which
# needs none of these and produces the same response shape). Installing
# these alongside Module 2 (which also loads a ~1.1GB NER model into
# memory) on a machine with limited RAM can cause Module 2 to crash with
# no error - see docs/HOW_TO_RUN_AND_TEST.md. Uncomment only if you have
# the SinBERT weights and enough RAM to run both modules at once.
# scikit-learn>=1.3.0
# pandas>=2.0.0
# numpy>=1.24.0
# torch>=2.0.0
# transformers>=4.35.0